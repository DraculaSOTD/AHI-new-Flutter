# AHI SDK Credentials - DO NOT COMMIT
# This file contains paths to sensitive AHI SDK credentials that should never be committed to version control

# Android Maven S3 Credentials
android/gradle.properties

# iOS Git Credentials (embedded in Podfile)
ios/Podfile

# Flutter SDK Configuration (contains JWT token)
lib/src/ahi_turnkey_services/ahi_sdk_config.dart

# Alternative: Store credentials in environment variables or secure storage
# Instructions for production deployment:
# 1. Move credentials to environment variables
# 2. Use Flutter's --dart-define for build-time configuration
# 3. Implement secure credential storage service
# 4. Never hardcode credentials in source files

## Recommended .gitignore additions:
# Add these lines to your main .gitignore file:

# AHI SDK Credentials
**/gradle.properties
**/ahi_sdk_config.dart
**/Podfile.lock

# Environment files
.env
.env.local
.env.production

# Secure credential files
**/credentials/
**/secrets/
